/**
 * Core TypeScript types for the LexMechanic Adeptus Mechanicus dataset.
 * These types are automatically derived from Zod schemas to ensure
 * runtime validation and static type safety stay in sync.
 */

export type {
  UnitStats,
  WeaponProfile,
  Ability,
  Unit,
  RawUnit,
  RawWeapon,
  RawEntry,
  Dataset,
  ManualOverride,
} from "../schemas/index.js";
